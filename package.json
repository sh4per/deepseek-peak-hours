{
  "name": "deepseek-peak-hours",
  "version": "1.0.0",
  "private": true,
  "description": "Static page showing whether DeepSeek API off-peak pricing is active right now.",
  "type": "module",
  "scripts": {
    "test": "node --test"
  }
}
